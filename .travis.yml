sudo: required
services:
  - docker

script:
  - docker-compose build 
  - docker-compose up -d mongo
  - docker-compose up -d app
  - docker-compose up -d client
  #- docker build -t dextez/fullstack
  #- docker images dextez/fullstack